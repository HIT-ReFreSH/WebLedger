{
  "target": "${WL_METHOD}",
  "host": "${WL_HOST}",
  "wl-sql": "${WL_SQL_HOST}",
  "host-mysql": "database=${WL_SQL_DB?ledger}; server=${wl-sql}; port=${WL_SQL_PORT?3306}; user=${WL_SQL_USER?root}; password=${WL_SQL_PWD?root}; Persist Security Info=False; Connect Timeout=300",
  "access": "${WL_ACCESS}",
  "secret": "${WL_SECRET}"
}
