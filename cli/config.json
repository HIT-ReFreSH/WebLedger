{
  "target": "${WL_METHOD}",
  "host": "${WL_HOST}",
  "wl-sql": "${WL_SQL_HOST}",
  "host-mysql": "database=${WL_SQL_DB?ledger}; server=${wl-sql}; port=${WL_SQL_PORT?3306}; user=${WL_SQL_USER?ledger-bot}; password=${WL_SQL_PWD?password}; Persist Security Info=False; Connect Timeout=300",
  "access": "${WL_ACCESS}",
  "secret": "${WL_SECRET}"
}
