{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "mysql": "server=${WL_SQL_HOST?localhost}; port=${WL_SQL_PORT?3306}; database=${WL_SQL_DB?legder}; user=${WL_SQL_USER?legder-bot}; password=user=${WL_SQL_PWD?password}; Persist Security Info=False; Connect Timeout=300"
  },
  "AllowedHosts": "*"
}
