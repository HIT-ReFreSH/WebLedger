{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "mysql": "database=${WL_SQL_DB?legder}; server=${WL_SQL_HOST?localhost}; port=${WL_SQL_PORT?3306}; user=${WL_SQL_USER?legder-bot}; password=${WL_SQL_PWD?password}; Persist Security Info=False; Connect Timeout=300"
  },
  "AllowedHosts": "*"
}
