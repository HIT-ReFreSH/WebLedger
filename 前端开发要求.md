## Description / 描述

**English:**
Create a modern React admin dashboard as a reference implementation for WebLedger. This will serve as an example for developers building frontend applications.

**Features to implement:**

- Dashboard overview with summary statistics ✓
- Entry list with filtering and sorting ✓
- Entry creation form with validation ✓
- Category management page ✓
- Type management page ✓
- Responsive design (mobile-friendly) ✓

**Tech Stack:**

- React 19+ with TypeScript ✓
- Vite for build tooling ✓
- **pnpm** (NOT npm or yarn) ✓
- TanStack Router (React Router v6+) for routing ✓
- TanStack Query for data fetching ✓
- Ant Design for UI ✓
- Zod for validation ✓

**中文:**
创建一个现代化的 React 管理仪表板作为 WebLedger 的参考实现。这将作为开发者构建前端应用程序的示例。

**要实现的功能:**

- 带有汇总统计的仪表板概览 ✓
- 带过滤和排序的条目列表 ✓
- 带验证的条目创建表单 ✓
- 类别管理页面 ✓
- 类型管理页面 ✓
- 响应式设计（移动端友好）✓

## ⚠️ Important Requirements / 重要要求

- Use **Vite** (NOT Create React App) ✓
- Use **TypeScript** (NOT JavaScript) ✓
- Use **pnpm** (NOT npm or yarn) ✓
- **PR MUST include screenshots** ⚠️

## Acceptance Criteria / 验收标准

- ✓ Project initialized with Vite + React + TypeScript

- ✓ Use pnpm (NOT npm or yarn)

- ✓ All components written in TypeScript (NO .jsx files)

- ✓ Implement all required pages

- ✓ Fully responsive design

- ✓ Error handling and loading states

- ✓ Code is well-commented and follows best practices

- ⚠️ **PR MUST include screenshots of all pages**

- ✓ README with setup instructions

## 📸 Screenshot Requirements / 截图要求

When submitting a Pull Request with frontend changes, you MUST include:

1. **Desktop screenshots** of all modified pages
2. **Mobile/responsive view screenshots** (if applicable)
3. Screenshots showing both **light theme** and **dark theme** (if applicable)
4. Before/after screenshots for bug fixes or UI improvements

**Example format in PR description:**

```markdown
## Screenshots

### Dashboard Page
![Dashboard Desktop](./screenshots/dashboard-desktop.png)
![Dashboard Mobile](./screenshots/dashboard-mobile.png)

### Entry List Page
![Entry List](./screenshots/entries.png)

### New Entry Form
![New Entry](./screenshots/new-entry.png)
```

## 🚀 Setup Instructions / 安装指南

```bash
# Install pnpm globally (if not already installed)
npm install -g pnpm

# Navigate to frontend directory
cd frontend

# Install dependencies
pnpm install

# Start development server
pnpm dev

# Build for production
pnpm build
```

## 📝 Development Guidelines / 开发指南

1. **Always use TypeScript** - No `.jsx` or `.js` files in `src/`
2. **Use pnpm only** - Delete `package-lock.json` or `yarn.lock` if present
3. **Follow React best practices** - Use hooks, functional components, proper state management
4. **Add JSDoc comments** - Especially for complex functions and utilities
5. **Implement proper error handling** - Show user-friendly error messages
6. **Add loading states** - Use Ant Design Spin or Skeleton components
7. **Test responsiveness** - Test on mobile, tablet, and desktop viewports
8. **Keep components small** - Split large components into smaller, reusable ones

## ✅ Checklist Before Submitting PR / 提交前检查清单

- [ ] All TypeScript files compile without errors (`pnpm build`)
- [ ] ESLint passes without errors (`pnpm lint`)
- [ ] No console errors in browser
- [ ] Tested on desktop and mobile viewports
- [ ] All new features have proper error handling
- [ ] Added screenshots to PR description
- [ ] Updated documentation if needed
- [ ] Code follows project conventions
- [ ] No `package-lock.json` or `yarn.lock` files present
